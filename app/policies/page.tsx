const policies=[{name:"Standard return window",value:"14 days",scope:"All non-final-sale items"},{name:"Auto approval",value:"Risk < 25",scope:"Eligible reasons inside policy window"},{name:"High-risk review",value:"Risk ≥ 70",scope:"Manual approval required"},{name:"Damaged inventory",value:"Quarantine",scope:"Exclude from sellable stock"}];
export default function Page(){return <main><div className="section-title">Policy controls</div><h1 style={{fontSize:38}}>Return policy as explicit rules.</h1><p className="muted" style={{maxWidth:720}}>Keeping policy decisions outside UI components makes changes reviewable and reduces accidental behavior drift.</p><div className="grid-auto" style={{marginTop:20}}>{policies.map(p=><div className="card" style={{padding:20}} key={p.name}><span className="muted">{p.name}</span><strong style={{display:"block",fontSize:26,margin:"10px 0"}}>{p.value}</strong><p className="muted">{p.scope}</p></div>)}</div><section className="card" style={{padding:20,marginTop:18}}><div className="section-title">Change-management note</div><p>In a production integration, policy edits would be versioned, audited and tested against historical cases before activation.</p></section></main>}
