import { returns } from "@/lib/data";
export default function Page(){const reasons=Object.entries(returns.reduce<Record<string,number>>((a,r)=>(a[r.reason]=(a[r.reason]||0)+1,a),{})).sort((a,b)=>b[1]-a[1]);return <main><div className="section-title">Returns intelligence</div><h1 style={{fontSize:38}}>Why customers send products back</h1><div className="grid-auto">{reasons.map(([reason,n])=><div className="card" style={{padding:20}} key={reason}><span className="muted">{reason}</span><strong style={{display:"block",fontSize:30,marginTop:8}}>{n}</strong><div style={{height:8,background:"#0a1019",borderRadius:8,marginTop:14}}><div style={{height:"100%",width:`${Math.min(100,n/returns.length*100)}%`,background:"var(--accent)",borderRadius:8}}/></div></div>)}</div></main>}
