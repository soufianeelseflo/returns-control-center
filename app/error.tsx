"use client"; export default function Error({reset}:{error:Error&{digest?:string};reset:()=>void}){return <main><div className="card" style={{padding:24}}><div className="section-title">Something went wrong</div><h1>Operations view failed safely.</h1><p className="muted">No destructive action was attempted. Retry the route when ready.</p><button className="btn btn-primary" onClick={reset}>Retry</button></div></main>}
